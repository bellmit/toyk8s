apiVersion: v1
kind: ReplicationController
metadata:
  name: ted-rc
spec:
  replicas: 2
  selector:
    app: python
  template:
    metadata:
      labels:
        app: python
    spec:
      containers:
      - name: hellopy
        image: ski2per/hellopy
        ports:
        - containerPort: 80
        

